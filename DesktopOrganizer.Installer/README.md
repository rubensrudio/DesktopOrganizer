# DesktopOrganizer.Installer

Placeholder project for the WiX Toolset v4 installer.

The actual installer (Package.wxs, Shortcuts.wxs, MSBuild integration) will be
implemented in TASK-026. For now this is a minimal `classlib` so the solution
restores and builds without errors.
